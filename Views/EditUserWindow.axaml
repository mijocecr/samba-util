<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        x:Class="SambaUtil.Views.EditUserWindow"
        Width="420" Height="460"
        Title="Edit User"
        CanResize="False"
      
        Background="{StaticResource BackgroundBrush}"
        Foreground="{StaticResource ForegroundBrush}">

    <Border Margin="20"
            Padding="20"
            CornerRadius="12"
            Background="{StaticResource ControlBackgroundBrush}"
            BorderBrush="{StaticResource BorderBrush}"
            BorderThickness="1"
            Effect="drop-shadow(0 4 14 #00000055)">

        <StackPanel Spacing="16">

            <!-- Título -->
            <TextBlock Text="User File"
                       FontSize="22"
                       FontWeight="Bold"
                       HorizontalAlignment="Center"
                       Foreground="{StaticResource ForegroundBrush}"/>

            <!-- Username -->
            <StackPanel Spacing="4">
                <TextBlock Text="Username"
                           FontWeight="Bold"
                           Foreground="{StaticResource ForegroundBrush}"/>
                <TextBox x:Name="UsernameText"
                         IsReadOnly="True"
                         CornerRadius="6"
                         Background="{StaticResource ControlBackgroundBrush}"
                         BorderBrush="{StaticResource BorderBrush}"
                         Foreground="{StaticResource ForegroundBrush}"/>
            </StackPanel>

            <!-- UID Samba -->
            <StackPanel Spacing="4">
                <TextBlock Text="Samba UID"
                           FontWeight="Bold"
                           Foreground="{StaticResource ForegroundBrush}"/>
                <TextBox x:Name="SambaUidText"
                         IsReadOnly="True"
                         CornerRadius="6"
                         Background="{StaticResource ControlBackgroundBrush}"
                         BorderBrush="{StaticResource BorderBrush}"
                         Foreground="{StaticResource ForegroundBrush}"/>
            </StackPanel>

            <!-- UID UNIX -->
            <StackPanel Spacing="4">
                <TextBlock Text="UNIX UID"
                           FontWeight="Bold"
                           Foreground="{StaticResource ForegroundBrush}"/>
                <TextBox x:Name="UnixUidText"
                         IsReadOnly="True"
                         CornerRadius="6"
                         Background="{StaticResource ControlBackgroundBrush}"
                         BorderBrush="{StaticResource BorderBrush}"
                         Foreground="{StaticResource ForegroundBrush}"/>
            </StackPanel>

            <!-- Estado -->
            <StackPanel Orientation="Horizontal" Spacing="10">
                <TextBlock Text="Enabled:"
                           VerticalAlignment="Center"
                           Foreground="{StaticResource ForegroundBrush}"/>
                <CheckBox x:Name="EnableCheck"/>
            </StackPanel>

            <!-- Nueva contraseña -->
            <StackPanel Spacing="4">
                <TextBlock Text="New Password"
                           FontWeight="Bold"
                           Foreground="{StaticResource ForegroundBrush}"/>
                <TextBox x:Name="NewPasswordBox"
                         PasswordChar="●"
                         CornerRadius="6"
                         Background="{StaticResource ControlBackgroundBrush}"
                         BorderBrush="{StaticResource BorderBrush}"
                         Foreground="{StaticResource ForegroundBrush}"/>
            </StackPanel>

            <!-- Botones -->
            <StackPanel Orientation="Horizontal"
                        Spacing="10"
                        VerticalAlignment="Center"
                        HorizontalAlignment="Right">

                <Button Content="Cancel"
                        Width="90"
                        HorizontalContentAlignment="Center"
                        
                        Classes="NeutralButton"
                        
                        Click="OnCancel"/>

                <Button Content="Save"
                        Width="90"
                        HorizontalContentAlignment="Center"
                        
                        
                        Classes="AccentButton"
                        Click="OnSave"/>

            </StackPanel>

        </StackPanel>
    </Border>
</Window>
